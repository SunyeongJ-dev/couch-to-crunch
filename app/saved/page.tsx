import VideoGrid from "../ui/video-grid";

export default function SavedPage() {
  return (
    <div className="flex w-full flex-col max-w-7xl mx-auto px-8 pt-4 pb-8">
      {/* <h1 className="text-2xl font-bold mb-8">Saved Videos</h1>
      {videos.filter((video) => savedVideos.includes(video.id)).length === 0 ? (
        <p className="text-text-500">No saved videos yet.</p>
      ) : (
        <VideoGrid
          videos={videos.filter((video) => savedVideos.includes(video.id))}
        />
      )} */}
    </div>
  );
}
